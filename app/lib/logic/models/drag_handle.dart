/// Represents the active handle when dragging or resizing a component bounding box.
enum DragHandle {
  none,
  topLeft,
  topRight,
  bottomLeft,
  bottomRight,
  top,
  bottom,
  left,
  right,
  center,
}
